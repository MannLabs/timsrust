[package]
name = "timsrust-parquet-spectra"
version = "0.1.0"
edition = "2024"

[dependencies]
timsrust-core = { workspace = true, features = ["io"] }
serde = { workspace = true }

[lints]
workspace = true
